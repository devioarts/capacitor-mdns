'use strict';

/**
 * Metadata consumed by the capacitor-electron runtime generator (`npm run update`).
 *
 * Import path for the JS layer: `@devioarts/capacitor-mdns/electron/settings`.
 */
const pluginSettings = {
    pluginClass: 'mDNS',
    pluginMethods: ['getPluginPlatform', 'startBroadcast', 'stopBroadcast', 'discover'],
    pluginEvents: [],
};

exports.pluginSettings = pluginSettings;
//# sourceMappingURL=plugin-settings.js.map
